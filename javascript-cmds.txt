📂 Execução e Ambiente
node <arquivo.js> = executa um arquivo JavaScript com Node.js
node --version = verifica versão do Node
npm --version = verifica versão do npm
node REPL = executar 'node' sem argumentos para abrir o REPL interativo
npx <package> = executa um pacote npm sem instalar globalmente
npm init -y = inicializa package.json com valores padrão
npm install <pacote> = instala dependência (salva em node_modules)
npm install --save-dev <pacote> = instala dependência de desenvolvimento
npm install -g <pacote> = instala globalmente

🖥️ Console e Depuração
console.log(valor) = imprime no console
console.error(valor) = imprime erro no console
console.warn(valor) = imprkime aviso
console.table(obj) = mostra tabela
console.dir(obj) = exibe objeto detalhado
debugger = ponto de interrupção para depurador
/* Usar DevTools (F12) -> Console / Sources para depurar em navegadores */

🔤 Variáveis e Tipos
let = variável de escopo de bloco (podem mudar somente no bloco)
const = variáveis que não é possivel reatribuir valor
var = variável com hoisting, podem mudar (evitar em ES6+)
typeof valor = retorna tipo (e.g. 'string', 'number', 'object', 'undefined')
null = ausência intencional de valor
undefined = valor não inicializado

➕ Operadores
+ - * / % = operadores aritméticos
++ -- = incremento / decremento
== vs === = igualdade e igualdade verificando o tipo e valor (== converte tipos, === estritamente igual)
!= vs !== = diferente e diferente verificando o tipo e valor
&& || ! = e, ou e negação (operadores lógicos)
?: = operador ternário (condição ? valorSeVerdadeiro : valorSeFalso)
< > <= >= =  menor que, maior que, menor ou igual e maior ou igual 
: = senão

🔁 Controle de Fluxo
if (condição) { ... } else { ... }
switch (valor) { case x: ...; break; default: ... }
for (let i=0; i<10; i++) { ... } = laço de repetição
for (const item of iterable) { ... } = itera valores
for (const chave in objeto) { ... } = itera chaves (objetos)
while (condição) { ... }
do { ... } while (condição)

🔧 Funções
function soma(a, b) { return a + b; } = declaração de função
const soma = (a, b) => a + b = arrow function (mais concisa)
function* geradora() { yield 1; } = generator function
(...args) = rest parameters (reúne argumentos)
(...arr) = spread operator (expande arrays/objetos)
function nome(array) {return Math.max.apply(null, array)} = retorna o maior número e o menor (min)
function filtragem(value, index, array) {return value > 20;} = retorna valores maiores que 20 

🧰 Arrays (métodos comuns)
const arr [1,2,3] = para criar uma matriz indexada (vários valores em uma única variável)
arr.push("valor") = acrescenta um valor ao final da matriz
arr.length = "valor" = mostra o indice da matriz
Array.isArray(nome da variavel) = retorna a confirmação se é uma váriavel ou não (true/false)
arr.pop() = remove do final
arr.shift() = remove do início
arr.unshift(x) = adiciona no início
arr.map(fn) = transforma cada elemento
arr.filter(fn) = filtra elementos
arr.reduce((acc,cur)=>..., init) = reduz para um valor
arr.find(fn) = encontra o primeiro que satisfaz
arr.includes(x) = verifica existência
arr.splice(indice, deleteCount, items...) = altera o array
arr.length = retorna a quantidade de elementos dentro da array
arr.length - 1 = retorna o último elemento dentro de uma lista 
join(" ") = modifica o separador entre os elementos
delete nome[0]; = exclui o campo e o torna undefined
arr.concat(arr2, arr3...) = concatena os arrays 
arr.slice(indice1, indice2) = divide os elementos apartir do indice
arr.sort() = ordem alfabética
arr.reverse() = ordem alfabética de trás para frente
arr.sort(function (a,b) {return a - b}) = enumera em ordem crescrente ou decrescente (b - a)
  
📦 Objetos e Classes
class nome = cria classe
{constructor(valor 1, valor 2...) = inicializa as propriedades do objeto
this.propriedade1 = valor1 this.propriedade = valor1 = guarda os valores recebidos
const obj = { nome: "Kaik", idade: 20 }
obj.chave = valor = adicionar/alterar propriedade
delete obj.chave = remove propriedade
Object.keys(obj) = array com chaves
Object.values(obj) = array com valores
Object.entries(obj) = array de [chave, valor]
{ ...obj } = clona/espalha objeto (spread)

🔗 Módulos
// CommonJS (Node)
const fs = require('fs')
module.exports = { minhaFunc }

// ES Modules (ESM)
import fs from 'fs'
export default MinhaFunc
export const nome = 'Kaik'

🌐 DOM (Browser)
alert(valor) = imprime um alerta
document.write(valor) = imprime um valor (usado mais para testes)
document.querySelector('.classe') = seleciona primeiro elemento
document.querySelectorAll('div') = NodeList (vários elementos)
document.getElementById('id') = seleciona um id
document.getElementById('id').innerHTML = "imprimir" = imprime na tela 
element.textContent = texto sem HTML
element.innerHTML = imprime um valor na tela/pega HTML (cuidado com XSS)
element.classList.add('ativa') = gerencia classes
element.addEventListener('click', fn) = escuta eventos
document.createElement('div') = cria elemento
document.body.style.backgorundColor = modifica a cor do fundo
parent.appendChild(child) = adicionar ao DOM
var = var.toLowerCase(); = converte em minusculo
setTimeout (funcion, tempo) = executa uma depois de esperar um tempo especificado em milliseconds
clearTimeout(id) = encerra a contagem 
setInterval(funcion,tempo) = executa uma ação continuamente a cada tempo determinado em milliseconds
clearInterval(funcion) = para a execução

Manipulação de Datas
new Data() = comando básico para pegar a data
new Date().getHours() = pegar a hora atual
data.getMinutes() = pegar os minutos de 0 a 59
data.getSeconds() = pegar os segundos de 0 a 59
data.getMilliseconds() = pegar os milisegundos de 0 a 999
new Date().getDay() = pegar o dia da semana de 0 a 6
new Date().getFullYear() = pegar o ano atual com 4 digitos
data.getMonth() = pegar o mês atual de 0 ate 11
data.getDate() = pegar o dia do mês
data.toLocaleString('pt-BR') = pegar a data e hora no padrão brasileiro
data.toLocaleString('pt-BR', {dateStyle: 'short'}) = pegar somente a data encurtado no padrão brasileiro
data.toLocaleString('pt-BR', {timeStyle: 'short'}) = pegar a hora encrutada no padrão brasileiro
function addZero(x) { return x < 10 ? '0' + x : '' + x; } = função para adicionar 0 na data

🛰️ Eventos
onclick = disparado quando recebe um clique
ondbclick = disparado quando clique duplo
onmouseover =  disparado quando o mouse está sobre
onmouseout = disparado quando o mouse é movido para fora do elemento
onmousemove = disparado quando o mouse é movido no elemento
onmousedown =  disparado quando o clique do botão for pressionado
onmouseup = disparado quando o clique do botão é liberado
onfocus = disparado quando o elemento recebe o foco (válido para input)
onchange = disparado quando existe uma mudança no conteudo
onblur = disparado quando o elemento perde o foco
onkeydown = disparado quando uma tecla é pressionada
onkeypress = disparado quando uma tecla é pressionada e solta
onkeyup = disparado quando uma tecla é solta sobre um elemento
onload = disparado quando a página terminar de ser carregada (body)
onresize = disparado quando há um redimencionamento da janela

event.preventDefault() = impede comportamento padrão (ex: submit)
event.stopPropagation() = impede propagação do evento
event.target = elemento que originou o evento
event.currentTarget = elemento com o listener

📡 Fetch / Requisições HTTP
fetch(url)
  .then(res => res.json())
  .then(data => ...)
  .catch(err => ...)

async function pegaDados() {
  try {
    const res = await fetch(url)
    const data = await res.json()
    return data
  } catch (err) {
    console.error(err)
  }
}

⚡ Promises / Async
new Promise((resolve, reject) => { ... })
Promise.all([p1, p2]) = resolve quando todos resolvem
Promise.race([p1, p2]) = resolve/reject com o primeiro
async / await = sintaxe assíncrona imperativa

🧪 Testes (comuns)
npm install --save-dev jest
npx jest = executa testes com Jest
mocha, chai = alternativas populares

🔁 Ferramentas de Build e Bundlers
npx create-react-app my-app = gera app React (CRA)
npm run build = script de build (dependendo do package.json)
webpack, parcel, rollup, vite = bundlers / dev servers
babel = transpila código moderno para compatibilidade

🧭 Source Maps e Transpilação
// para debugging em código transpilado
// gerar source maps para mapear código original no devtools

🧩 Estruturas ES6+ úteis
const { a, b } = obj = destructuring de objeto
const [x, y] = arr = destructuring de array
...rest e ...spread = rest/spread operator
class Pessoa { constructor(nome){ this.nome = nome } } = classes
static metodo() {} = método estático
get/set = getters e setters

🗺️ Map, Set, WeakMap, WeakSet
const mapa = new Map()
mapa.set(chave, valor)
mapa.get(chave)
const conjunto = new Set([1,2,3])
set.has(x), set.add(x), set.delete(x)

🛡️ Segurança e Boas Práticas
evitar innerHTML com conteúdo não confiável (XSS)
usar HTTPS para requisições
validar e sanitizar entradas do usuário
tratar erros em Promises e async/await

🔍 Outras Dicas Úteis
var = new XMLHttpRequest(); var.open('GET', 'link'); var.send(); = para fazer uma requisição a um site
var.onload = ação ao carregar as informações do site
JSON.stringify() = converte objetos em texto no padrão JSON
JSON.parse() = converte texto no padrão JSON
typeof NaN === 'number'
Number('123') = converter para número
String(123) = converter para string
parseInt('10', 10), parseFloat('3.14') = converte para número inteiro e quebrado
Math.random(), Math.floor(), Math.ceil(), Math.abs()
Date.now(), new Date()
Intl.DateTimeFormat para formatação de datas

📚 Recursos Rápidos (comandos de terminal)
npm run start = geralmente inicia server de desenvolvimento
npm run test = executa testes
npm run lint = executa linter (eslint)
npm install --save <pacote> = adiciona dependência de produção